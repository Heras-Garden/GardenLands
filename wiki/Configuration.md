# Configuration

GardenLands config controls chat formatting, territory cache refresh, container permissions, and BlueMap integration.

Important chat settings include the `>>` separator, owner color, staff color, territory flags, and player heads.

Container permissions should remain enabled if GardenLands is the server's claim protection system.

BlueMap is optional. When enabled, GardenLands publishes territory, city, district, and property markers.